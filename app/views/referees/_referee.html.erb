<li> <%= link_to referee.name,referee%> 
     <%= link_to "edit", edit_referee_path(referee) %>
     <%= link_to "delete", referee_path(referee), method: :delete, data: {confirm: "Are you sure you want to delete #{ referee.name }?"} %>
</li>